{
  "name": "03-e2e-tests",
  "description": "Workshop Section 3 — End-to-End Tests",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "test:headed": "SLOWMO=1 playwright test --headed",
    "test:ui": "playwright test --ui --ui-host=localhost",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
