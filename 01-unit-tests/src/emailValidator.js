/**
 * Returns true if the given string looks like a valid email address.
 *
 * ⚠️  This implementation is intentionally incomplete.
 *     Your job is to find the bugs and fix them.
 *
 * Run the tests first to see what's broken:
 *   npm test
 */
export function isValidEmail(email) {
  // This check is too simple — what cases does it miss?
  return email.includes('@')
}
