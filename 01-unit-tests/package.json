{
  "name": "01-unit-tests",
  "description": "Workshop Section 1 — Unit Tests",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
